# Shared mypy configuration for all PCAI repos (canonical in pcai_utils/).
# Hardlinked into each repo root as mypy.ini — edit here, propagate everywhere.
[mypy]
python_version = 3.14
ignore_missing_imports = True