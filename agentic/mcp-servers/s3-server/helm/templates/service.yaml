apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-s3-server.fullname" . }}
  labels:
    {{- include "mcp-s3-server.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcp-s3-server.selectorLabels" . | nindent 4 }}

